#!/usr/bin/python3
def print_matrix_integer(matrix=[[]]):
    for innner_list in matrix:
        lent = len(inner_list)
        for i in range(lent)
            if i < lent - 1
                print("{:d}".format(i), end=" ")
            else:
                print("{:d}".format(i))
